const FREE_WARNING = 'Free shipping only applies to single customer orders';
const BANNED_WARNING = 'Unfortunately we do not ship to your country of residence';
const NONE_SELECTED = 0;

let RSA;
let shipping;

shipping ='rsa'; if (shipping = 'rsa') {shipping === 400 && currency === 'R'}
console.log ('shipping');

let NAM;
shipping = 'nam'; if ( shipping = 'nam') {shipping === 600 && currency === '$'} else { shipping === 800 && currency === '$'}
console.log('shipping');

let shoes = 300 * 1;
let toys = 100 * 5;
let shirts = 150 * 'NONE_SELECTED'
let batteries = 35 * 2
let pens = 5 * 'NONE_SELECTED' 

shipping = null
let currency = '$';

 /*if (shoes + batteries + pens + shirts > 1000 && currency === '$'){
	if (location = NAM && customers < 2) {
			if (location = RSA)
		    shipping = 0 || calcShipping
		}
	} 
console.log (calcShipping);*/


//if (shipping = 0 && customers !== 1) 
//console.log(FREE_WARNING);

let location = 'NK';
if (location = 'NK'){console.log(BANNED_WARNING)} else {console.log('price', currency, shoes + batteries + pens + shirts)};

let customers = 1
//let location = 'RSA'
currency = null